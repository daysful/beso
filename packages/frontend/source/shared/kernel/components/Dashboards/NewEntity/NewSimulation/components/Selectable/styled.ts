// #region imports
    // #region libraries
    import styled from 'styled-components';

    import {
        Theme,
    } from '@plurid/plurid-themes';
    // #region libraries
// #region imports



// #region module
export interface IStyledSelectable {
    theme: Theme;
}

export const StyledSelectable = styled.div<IStyledSelectable>`
`;
// #region module
